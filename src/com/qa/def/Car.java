package com.qa.def;
//Default access modifiers for access between different classes
public class Car {
	
	//default as the access modifier if none is specified,
	//meaning exhausts doesn't have private or public before it
	int exhausts = 4;
	// other classes can access this variable above
	
}
